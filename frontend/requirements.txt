# CyMOPS Frontend — npm Dependencies (Phase 3 additions)
# Run: npm install

# ── Phase 3: Jira-style Issue Tracking ──
@dnd-kit/core          # Drag-and-drop context and sensors
@dnd-kit/sortable      # Sortable context and useSortable hook
@dnd-kit/utilities     # CSS.Transform utilities
@dnd-kit/modifiers     # restrictToWindowEdges modifier (fixes drag-overlay offset)

# ── Existing (already in package.json) ──
react
react-dom
react-router-dom
axios
lucide-react
@stomp/stompjs
sockjs-client
